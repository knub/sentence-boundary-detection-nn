[word_vector]
# if set to 'avg' the average word vector is taken,
# otherwise the given word is taken as key error vector.
key_error_vector = this

[windowing]
window_size = 5
punctuation_position = 3

[features]
use_question_mark = true
pos_tagging = false

[data]
normalize_class_distribution = false
